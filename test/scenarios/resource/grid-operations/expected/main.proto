// Generated by Microsoft TypeSpec

syntax = "proto3";

package main;

import "google/api/annotations.proto";
import "resource.proto";
import "google/protobuf/empty.proto";

message Widget {
  string id = 1;
  int32 weight = 2;
  string color = 3;
}

message WidgetQuery {}

service Widgets {
  // Gets an instance of the Widget.
  rpc Get(resource.WidgetRequestParameter) returns (resource.WidgetResponse) {
    option (google.api.http) = {
      get: "/widgets/{id}"
    };
  }
  // Updates an existing instance of the Widget.
  rpc Update(resource.WidgetUpdateRequest) returns (resource.WidgetResponse) {
    option (google.api.http) = {
      patch: "/widgets/{id}"
      body: "*"
    };
  }
  // Deletes an existing instance of the Widget.
  rpc Delete(resource.WidgetRequestParameter) returns (google.protobuf.Empty) {
    option (google.api.http) = {
      delete: "/widgets/{id}"
    };
  }
  // Lists all instances of the Widget.
  rpc List(resource.WidgetQueryListSearchRequest) returns (resource.WidgetCollectionWithCountResponse) {
    option (google.api.http) = {
      get: "/widgets"
    };
  }
  // Creates a new instance of the Widget.
  rpc Create(resource.WidgetCreateRequest) returns (resource.WidgetResponse) {
    option (google.api.http) = {
      post: "/widgets"
      body: "*"
    };
  }
  // Creates new instances of the Widget.
  rpc CreateMany(resource.WidgetCreateCollectionRequest) returns (resource.WidgetCollectionResponse) {
    option (google.api.http) = {
      put: "/widgets"
      body: "*"
    };
  }
  // Updates existing instances of the Widget.
  rpc UpdateMany(resource.WidgetUpdateCollectionRequest) returns (resource.WidgetCollectionResponse) {
    option (google.api.http) = {
      patch: "/widgets"
      body: "*"
    };
  }
  // Deletes existing instances of the Widget.
  rpc DeleteMany(resource.WidgetDeleteCollectionRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {
      delete: "/widgets"
      body: "*"
    };
  }
}
